apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kosync-redis
  namespace: kosync
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
